spring:
  application:
    name: seen-busi
  profiles:
    active: localtest
---
spring:
  config:
    activate:
      on-profile: localtest
    import: file:../../localtest/common.yml
server:
  port: 9102